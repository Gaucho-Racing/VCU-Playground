// if there is any error, then don't charge

//already checked not moving, no errors (voltage)
bool chargeOrNot () {
    if () {  //if something is wrong, abort
        return false;
    } else {  //change state to charging
        return true;
    }
}

int state_4_do_not_charge() {
   //send warning to dashboard
   //loop until unplugged, on which return to fail state
}
